version: "3.6"

services:
  eiffelactory:
    image: {{ docker_image }}
    volumes:
      - type: bind
        source: ./conf
        target: /app/conf
    networks:
      - eiffelactorynet

networks:
  eiffelactorynet:
